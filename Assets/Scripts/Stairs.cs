using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Stairs : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Not sure how you're gonna set this up, but I had it envisioned to swap stair directions & disable the box collider when the puzzle was solved.
    // Message me if you've got any concerns. -Brendan

    // Update is called once per frame
    void Update()
    {
        
    }
}
